extends layout

block content

  h1 #{title}: #{'for' + ' ' + book.title}
  h2 Instance ID: #{bookinstance._id}
  p Status: #{bookinstance.status}
  p Due back : #{bookinstance.due_back_formatted}
  
  p Do you really want to delete this Book Instance?

    form(method='POST')
      div.form-group
        input#bookinstanceid.form-control(type='hidden', name='bookinstanceid', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete
